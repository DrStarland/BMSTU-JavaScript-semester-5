Задание 1
С клавиатуры считываются числа A и B. Необходимо вывести на экран все числа Фибоначчи,
которые принадлежат отрезку от A до B.

Задание 2
С клавиатуры считываются числа A и B. Необходимо вывести на экран все числа,
квадратный корень которых является целым числом. При этом, необходимо вывести только числа,
которые принадлежат отрезку от A до B.

Задание 3
С клавиатуры считываются числа A, B и C. Необходимо вывести на экран все числа,
которые принадлежат отрезку от A до B и делятся на C без остатка.
Также надо вывести на экран сумму полученных чисел.

Для всех X и Z, X — предок Z, если X — родитель Z.

predecessor( X, Z) :- parent( X, Z).

X что-то для Z, если X другое что-то для Z

циклы делаются через рекурсию после условий

! - оператор отсечения

line(X, Y, A, B) :- checkInRange(Y, A, B), Z is X + Y, X_NEW is Y, Y_NEW is Z, Y_NEW =< B, fibb(X_NEW, Y_NEW, A, B).
checkInRange(X, A, B) :- X_NEW is X, (A =< X, X =< B, write(X_NEW), write(" "), !; write("")).	